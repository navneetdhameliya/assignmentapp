import 'package:flutter/material.dart';

class SizedBox16Height extends StatelessWidget {
  const SizedBox16Height({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16.0,
    );
  }
}

class SizedBox16Width extends StatelessWidget {
  const SizedBox16Width({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 16.0,
    );
  }
}

class SizedBox8Height extends StatelessWidget {
  const SizedBox8Height({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 8.0,
    );
  }
}

class SizedBox8Width extends StatelessWidget {
  const SizedBox8Width({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 8.0,
    );
  }
}
